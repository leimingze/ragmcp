"""Evaluation base abstractions."""

from abc import ABC, abstractmethod
from typing import Any


class Evaluator(ABC):
    """Abstract base class for evaluation implementations.

    Provides unified interface for evaluating RAG pipeline quality
    and computing standardized metrics.
    """

    @abstractmethod
    def evaluate(
        self,
        query: str,
        retrieved_chunks: list[Any],
        generated_answer: str,
        ground_truth: str,
    ) -> dict[str, float]:
        """Evaluate the quality of retrieval and generation.

        Args:
            query: The user's search query.
            retrieved_chunks: List of chunks retrieved for the query.
            generated_answer: The answer generated by the RAG system.
            ground_truth: The reference/expected answer.

        Returns:
            Dictionary of metric names to float values (e.g., {"precision": 0.85}).
        """
        ...
